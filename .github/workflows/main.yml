name: Label new issues

on:
  issues:
      types: [opened]

      jobs:
        label:
            runs-on: ubuntu-latest

                steps:
                    - name: Label new issues
                          uses: actions-ecosystem/action-add-labels@v1
                                with:
                                        labels: 'new-issue'
